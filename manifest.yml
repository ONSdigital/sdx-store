applications:
- name: sdx-store
  buildpack: python_buildpack
  health-check-http-endpoint: /info
  health-check-type: http

  env:
    CF_DEPLOYMENT: True
    CREATE_TABLES: True

  services:
   - sdx-postgres-dev
